# 泰嘉金属官网更新包使用说明

本次更新完成了低眩光钢铁风视觉、文字对比度优化、新工厂实拍图区域、加工流程切换、图片分类筛选及大图浏览等功能。

## 应该使用哪个压缩包

- **已有 GitHub 网站：** 使用“小型更新包”。
- **重新部署或需要完整备份：** 使用“完整上传包”。

## GitHub 上传方法

1. 先把 ZIP 解压到电脑文件夹中。
2. 打开 GitHub 仓库 `yunzheshen2023-star/taijia-metal-website`。
3. 点击 **Add file → Upload files**。
4. 将解压后的文件和文件夹拖入上传区域。不要直接上传 ZIP 文件。
5. 如果页面询问是否替换 `index.html`，请选择替换。
6. 确认仓库最外层可以直接看到 `index.html`、`steel-refresh.css`、`factory-journal.js` 和 `assets` 文件夹。
7. 在页面底部点击 **Commit changes**。
8. 如果 Netlify 已连接该 GitHub 仓库，提交后通常会自动开始部署。等待部署状态显示 Published 后，刷新官网即可。

## 小型更新包包含

- 更新后的 `index.html`
- 新钢铁风样式 `steel-refresh.css`
- 新工厂实拍互动脚本 `factory-journal.js`
- `assets/factory-journal` 内 12 张筛选并优化用于网页展示的工厂实拍图

## 注意事项

- 上传前建议在 GitHub 中保留当前版本，便于需要时恢复。
- 上传的是 ZIP 解压后的内容，不是 ZIP 压缩包本身。
- 本次没有删除官网原有行业和产品图片。
- 页面默认以英文为主要信息，中文作为辅助信息，并继续支持中英文切换。

---

English note: Extract the ZIP first, upload the extracted contents to the repository root, and commit the changes. If Netlify is connected to the repository, it should deploy automatically.
